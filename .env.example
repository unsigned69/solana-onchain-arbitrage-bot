# RPC, который использует ПАРСЕР (не раннер)
PARSER_RPC_ENDPOINT="https://api.mainnet-beta.solana.com"

# Резервный RPC, общий с ботом (используется, если PARSER_RPC_ENDPOINT не задан)
RPC_ENDPOINT="https://api.mainnet-beta.solana.com"

# Путь до основного конфигурационного файла бота
BOT_CONFIG_PATH="./config/token-picker.json"
